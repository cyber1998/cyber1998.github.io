{
  "name": "cyber1998.github.io",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --mode development --outDir dist",
    "build:prod": "vite build --mode production --outDir dist-prod",
    "build:local": "vite build --mode development --outDir dist-local",
    "preview": "vite preview --port 4173",
    "preview:local": "npm run build:local && vite preview --port 4173 --outDir dist-local",
    "prod": "npm run build:prod && vite preview --port 4173 --outDir dist-prod",
    "production": "npm run build:prod && vite preview --port 4173 --outDir dist-prod",
    "deploy": "npm run build:prod && echo 'Ready for GitHub Pages deployment from dist-prod/'"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.1.1",
    "svelte": "^5.38.1",
    "terser": "^5.44.0",
    "vite": "^7.1.2"
  }
}
